import React,{useMemo,useState} from 'react';
import{createRoot}from'react-dom/client';
import{AnimatePresence,motion}from'motion/react';
import{ArrowRight,ArrowUpRight,Bathtub,Buildings,CalendarBlank,ChartLineUp,Check,CirclesFour,Clock,Compass,EnvelopeSimple,Eye,FacebookLogo,Heart,House,InstagramLogo,List,MapPin,Minus,MagnifyingGlass,Phone,Plus,ShareNetwork,SlidersHorizontal,SwimmingPool,WhatsappLogo,X}from'@phosphor-icons/react';
import{agencies,developments,enquiries as seedEnquiries,properties,regions}from'./data';
import'./styles.css';
import'./advanced.css';

const euro=n=>new Intl.NumberFormat('en-IE',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(n);
const short=n=>n>=1e6?`€${(n/1e6).toFixed(n%1e6?1:0)}m`:`€${Math.round(n/1000)}k`;
const nav=[['Buy','/properties'],['New Developments','/developments'],['Explore Portugal','/#destinations'],['Agencies','/agencies'],['Buying Guide','/legal-support']];
const go=p=>{history.pushState({},'',p);window.dispatchEvent(new PopStateEvent('popstate'));scrollTo({top:0,behavior:'smooth'})};
function Link({to,children,className='',onClick}){return <a href={to} className={className} onClick={e=>{if(to.startsWith('/')){e.preventDefault();go(to)};onClick?.()}}>{children}</a>}
function Logo({light=false}){return <Link to="/" className={`logo ${light?'light':''}`}><span className="logo-mark">P</span><span>Properties<br/><b>in Portugal</b></span></Link>}
function Header(){const[open,setOpen]=useState(false);return <header className="header"><div className="shell nav"><Logo/><nav>{nav.map(([n,p])=><Link key={n} to={p}>{n}</Link>)}</nav><div className="nav-actions"><Link to="/account/favourites"><Heart/> Saved</Link><Link to="/agency" className="button small dark">Agency portal <ArrowUpRight/></Link></div><button className="menu" onClick={()=>setOpen(!open)} aria-label="Open menu">{open?<X/>:<List/>}</button></div><AnimatePresence>{open&&<motion.div initial={{height:0}} animate={{height:'auto'}} exit={{height:0}} className="mobile-nav">{nav.map(([n,p])=><Link key={n} to={p} onClick={()=>setOpen(false)}>{n}</Link>)}<Link to="/agency">Agency portal</Link></motion.div>}</AnimatePresence></header>}
function Footer(){return <footer><div className="shell footer-grid"><div><Logo light/><p>Exceptional homes, local expertise and clear guidance for your move to Portugal.</p></div><div><h4>Discover</h4><Link to="/properties">Properties</Link><Link to="/developments">New developments</Link><Link to="/agencies">Agencies</Link></div><div><h4>Buyer services</h4><Link to="/mortgage">Mortgage calculator</Link><Link to="/legal-support">Legal support</Link><Link to="/">Buying guide</Link></div><div><h4>Follow</h4><span className="socials"><InstagramLogo/><FacebookLogo/></span><p className="tiny">Prototype experience. Listings and analytics shown are seeded demo data.</p></div></div><div className="shell footer-bottom"><span>© 2026 Properties in Portugal</span><span>Privacy · Cookies · Terms</span></div></footer>}
function Shell({children}){return <><Header/><main>{children}</main><Footer/></>}
function Reveal({children,className='',delay=0}){return <motion.div className={className} initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.15}} transition={{duration:.55,delay}}>{children}</motion.div>}
function SearchBar({compact=false}){const[loc,setLoc]=useState('');const[type,setType]=useState('');return <div className={`searchbar ${compact?'compact':''}`}><label><span>Location</span><input value={loc} onChange={e=>setLoc(e.target.value)} placeholder="Town, region or postcode"/></label><label><span>Property type</span><select value={type} onChange={e=>setType(e.target.value)}><option value="">Any type</option><option>Villa</option><option>Apartment</option><option>House</option></select></label><label><span>Price</span><select><option>Any price</option><option>Up to €500k</option><option>€500k – €1m</option><option>€1m+</option></select></label><button onClick={()=>go(`/properties?location=${encodeURIComponent(loc)}&type=${type}`)} aria-label="Search"><MagnifyingGlass/> <span>Search</span></button></div>}
function PropertyCard({p,favs,setFavs}){const saved=favs.includes(p.id);return <article className="property-card"><div className="card-image" onClick={()=>go(`/properties/${p.slug}`)}><img src={p.image} alt={`${p.title} in ${p.city}`}/>{p.status!=='Live'&&<span className="status image-status">{p.status}</span>}<button className={`save ${saved?'saved':''}`} onClick={e=>{e.stopPropagation();setFavs(saved?favs.filter(x=>x!==p.id):[...favs,p.id])}} aria-label={saved?'Remove from saved':'Save property'}><Heart weight={saved?'fill':'regular'}/></button></div><div className="card-copy" onClick={()=>go(`/properties/${p.slug}`)}><div className="price">{euro(p.price)}</div><h3>{p.title}</h3><p><MapPin/> {p.city}, {p.region}</p><div className="specs"><span>{p.beds} beds</span><span>{p.baths} baths</span><span>{p.area} m²</span></div><div className="agency-mini"><span>{p.agency.mark}</span>{p.agency.name}</div></div></article>}
function useFavs(){const[favs,setState]=useState(()=>JSON.parse(localStorage.getItem('pip-favourites')||'[]'));const setFavs=v=>{setState(v);localStorage.setItem('pip-favourites',JSON.stringify(v))};return[favs,setFavs]}
function Home(){const[favs,setFavs]=useFavs();return <Shell><section className="hero"><img src="https://images.unsplash.com/photo-1600607687920-4e2a09cf159d?auto=format&fit=crop&w=2200&q=88" alt="Contemporary villa overlooking the Portuguese coast"/><div className="hero-scrim"/><div className="shell hero-inner"><motion.p initial={{opacity:0}} animate={{opacity:1}} className="eyebrow light">PORTUGAL, BEAUTIFULLY FOUND</motion.p><motion.h1 initial={{opacity:0,y:20}} animate={{opacity:1,y:0}}>Find your place<br/>in Portugal.</motion.h1><motion.p initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} transition={{delay:.1}}>Exceptional homes and new developments, brought together with trusted local expertise.</motion.p><SearchBar/></div></section><section className="section shell"><div className="section-heading"><p className="eyebrow">CURATED FOR YOU</p><h2>Homes worth crossing<br/>borders for.</h2><Link to="/properties" className="text-link">View all properties <ArrowRight/></Link></div><div className="property-grid">{properties.slice(0,6).map((p,i)=><Reveal key={p.id} delay={i*.04}><PropertyCard p={p} favs={favs} setFavs={setFavs}/></Reveal>)}</div></section><section id="destinations" className="section destinations"><div className="shell"><div className="section-heading"><p className="eyebrow">EXPLORE PORTUGAL</p><h2>Six regions.<br/>Endless ways to live.</h2></div><div className="destination-grid">{regions.map((r,i)=><Reveal className={`destination d${i}`} key={r.name}><img src={r.image} alt={r.name}/><div><h3>{r.name}</h3><p>{r.count} properties</p></div><button onClick={()=>go(`/properties?location=${r.name}`)} aria-label={`Explore ${r.name}`}><ArrowUpRight/></button></Reveal>)}</div></div></section><section className="section shell"><div className="development-feature"><img src={developments[0].image} alt="Atlantic Residences"/><div><p className="eyebrow">NEW DEVELOPMENTS</p><h2>Portugal’s next chapter is taking shape.</h2><p>Explore exceptional new-build homes from the Algarve to the Douro, with clear availability and considered detail.</p><Link to="/developments" className="button dark">Explore developments <ArrowRight/></Link></div></div></section><section className="section services"><div className="shell"><div className="section-heading"><h2>Make your move<br/>with confidence.</h2></div><div className="service-list"><Link to="/mortgage"><span>01</span><div><h3>Mortgage planning</h3><p>Understand deposits and estimated monthly costs.</p></div><ArrowUpRight/></Link><Link to="/legal-support"><span>02</span><div><h3>Legal and notary support</h3><p>A clearer route through the Portuguese purchase process.</p></div><ArrowUpRight/></Link><Link to="/agencies"><span>03</span><div><h3>Trusted local agencies</h3><p>Connect with professionals who know each place personally.</p></div><ArrowUpRight/></Link></div></div></section></Shell>}
function FilterSelect({value,onChange,label,options}){const[open,setOpen]=useState(false);return <div className="filter-select"><button type="button" onClick={()=>setOpen(!open)}>{value||label}<span>⌄</span></button>{open&&<div className="filter-menu">{options.map(o=><button type="button" key={o.value} className={value===o.value?'chosen':''} onClick={()=>{onChange(o.value);setOpen(false)}}>{o.label}{value===o.value&&<Check/>}</button>)}</div>}</div>}
function Filters({state,setState}){return <div className="filters"><FilterSelect value={state.location} label="All Portugal" onChange={v=>setState({...state,location:v})} options={[{label:'All Portugal',value:''},...regions.map(r=>({label:r.name,value:r.name}))]}/><FilterSelect value={state.type} label="Any property" onChange={v=>setState({...state,type:v})} options={[{label:'Any property',value:''},...['Villa','Apartment','House','Townhouse'].map(x=>({label:x,value:x}))]}/><FilterSelect value={state.max?`Up to ${euro(+state.max)}`:''} label="Any price" onChange={v=>setState({...state,max:v})} options={[{label:'Any price',value:''},{label:'Up to €500k',value:'500000'},{label:'Up to €750k',value:'750000'},{label:'Up to €1m',value:'1000000'},{label:'Up to €1.5m',value:'1500000'}]}/><FilterSelect value={state.beds?`${state.beds}+ beds`:''} label="Any beds" onChange={v=>setState({...state,beds:v})} options={[{label:'Any beds',value:''},{label:'2+ beds',value:'2'},{label:'3+ beds',value:'3'},{label:'4+ beds',value:'4'}]}/><button className="filter-button"><SlidersHorizontal/> More filters</button></div>}
function Properties(){const qs=new URLSearchParams(location.search);const[state,setState]=useState({location:qs.get('location')||'',type:qs.get('type')||'',max:'',beds:''});const[sort,setSort]=useState('Recommended');const[map,setMap]=useState(false);const[favs,setFavs]=useFavs();const result=useMemo(()=>properties.filter(p=>(!state.location||(p.region+' '+p.city).toLowerCase().includes(state.location.toLowerCase()))&&(!state.type||p.type===state.type)&&(!state.max||p.price<=+state.max)&&(!state.beds||p.beds>=+state.beds)).sort((a,b)=>sort==='Price low to high'?a.price-b.price:sort==='Price high to low'?b.price-a.price:a.id-b.id),[state,sort]);return <Shell><section className="search-page-head shell"><p className="eyebrow">PROPERTY SEARCH</p><h1>Homes for sale in Portugal</h1><p>{result.length} curated properties in this prototype collection</p><Filters state={state} setState={setState}/><div className="result-tools"><span>{result.length} results</span><div><select value={sort} onChange={e=>setSort(e.target.value)}><option>Recommended</option><option>Price low to high</option><option>Price high to low</option></select><button className={map?'':'active'} onClick={()=>setMap(false)}><CirclesFour/> Grid</button><button className={map?'active':''} onClick={()=>setMap(true)}><MapPin/> Map</button></div></div></section>{result.length?<section className={`results shell ${map?'map-mode':''}`}><div className="property-grid">{result.map(p=><PropertyCard key={p.id} p={p} favs={favs} setFavs={setFavs}/>)}</div>{map&&<MapView data={result}/>}</section>:<section className="empty shell"><Compass/><h2>No exact matches yet.</h2><p>Try expanding your location or price range.</p><button className="button dark" onClick={()=>setState({location:'',type:'',max:'',beds:''})}>Clear filters</button></section>}</Shell>}
function MapView({data}){const[pos,setPos]=useState(null);return <div className="map"><div className="map-label"><MapPin/> Explore the map <span>Approximate demo locations</span></div>{data.map((p,i)=><button key={p.id} onClick={()=>setPos(p)} style={{left:`${12+(i*23)%75}%`,top:`${18+(i*31)%65}%`}}>{short(p.price)}</button>)}{pos&&<div className="map-preview"><img src={pos.image}/><div><b>{short(pos.price)}</b><span>{pos.title}</span><small>{pos.city} · {pos.beds} beds</small></div><Link to={`/properties/${pos.slug}`}><ArrowRight/></Link></div>}</div>}
function EnquiryForm({subject}){const[sent,setSent]=useState(false);const submit=e=>{e.preventDefault();const arr=JSON.parse(localStorage.getItem('pip-enquiries')||'[]');localStorage.setItem('pip-enquiries',JSON.stringify([...arr,{subject,date:new Date().toISOString()}]));setSent(true)};return sent?<div className="success"><Check/><h3>Thank you. Your enquiry is saved.</h3><p>A property professional would follow up from here in production.</p></div>:<form onSubmit={submit} className="enquiry"><div className="field-row"><label>Name<input required placeholder="Your name"/></label><label>Email<input required type="email" placeholder="you@example.com"/></label></div><label>Phone<input placeholder="Country code and number"/></label><label>Message<textarea defaultValue={`I'm interested in ${subject}. Please send me more information.`}/></label><button className="button dark">Request information <ArrowRight/></button><small>Demo form. Your enquiry is stored locally in this browser.</small></form>}
function PropertyDetail({slug}){const p=properties.find(x=>x.slug===slug)||properties[0];const[favs,setFavs]=useFavs();const saved=favs.includes(p.id);const gallery=[p.image,...properties.filter(x=>x.id!==p.id).slice(0,4).map(x=>x.image)];return <Shell><section className="detail shell"><div className="crumb">Buy / {p.region} / {p.title}</div><div className="gallery">{gallery.slice(0,3).map((im,i)=><img key={im} className={`g${i}`} src={im} alt={`${p.title} view ${i+1}`}/>) }<button>{gallery.length} photos <Plus/></button></div><div className="detail-layout"><div><div className="property-title"><p>{p.city}, {p.region}</p><h1>{p.title}</h1><div className="price">{euro(p.price)}</div></div><div className="fact-row"><span><House/> {p.beds} bedrooms</span><span><Bathtub/> {p.baths} bathrooms</span><span><Buildings/> {p.area} m² interior</span></div><section className="prose"><h2>A considered way to live in {p.city}.</h2><p>{p.description}</p><p>Floor-to-ceiling openings connect the interior with terraces and landscaped gardens. The result is a relaxed, elegant home equally suited to permanent living and long Portuguese summers.</p></section><section><h2>Features</h2><div className="feature-grid">{p.features.map(f=><span key={f}><Check/> {f}</span>)}</div></section><section className="location-block"><div><p className="eyebrow">LOCATION</p><h2>{p.city}, {p.region}</h2><p>Well connected to local restaurants, daily essentials and the coast.</p></div><MapPin/></section></div><aside className="contact-card"><div className="agency-head"><span>{p.agency.mark}</span><div><b>{p.agency.name}</b><small>{p.agency.location}, Portugal</small></div></div><EnquiryForm subject={p.title}/><div className="contact-actions"><button><WhatsappLogo/> WhatsApp</button><button><Phone/> Call</button><button onClick={()=>setFavs(saved?favs.filter(x=>x!==p.id):[...favs,p.id])}><Heart weight={saved?'fill':'regular'}/> {saved?'Saved':'Save'}</button><button><ShareNetwork/> Share</button></div></aside></div><section className="similar"><h2>Similar homes</h2><div className="property-grid">{properties.filter(x=>x.id!==p.id).slice(0,3).map(x=><PropertyCard key={x.id} p={x} favs={favs} setFavs={setFavs}/>)}</div></section></section></Shell>}
function Developments(){return <Shell><section className="development-hero"><img src={developments[1].image}/><div className="hero-scrim"/><div><p className="eyebrow light">NEW DEVELOPMENTS</p><h1>Designed for<br/>what comes next.</h1><p>New-build residences and investment opportunities in Portugal’s most compelling locations.</p></div></section><section className="section shell"><div className="development-list">{developments.map((d,i)=><article key={d.slug}><div className="number">0{i+1}</div><img src={d.image} alt={d.name}/><div><p>{d.place}</p><h2>{d.name}</h2><p>{d.copy}</p><dl><div><dt>From</dt><dd>{euro(d.from)}</dd></div><div><dt>Residences</dt><dd>{d.beds}</dd></div><div><dt>Completion</dt><dd>{d.completion}</dd></div></dl><Link to={`/developments/${d.slug}`} className="text-link">Discover the development <ArrowRight/></Link></div></article>)}</div></section></Shell>}
function DevelopmentDetail({slug}){const d=developments.find(x=>x.slug===slug)||developments[0];const[selected,setSelected]=useState('');return <Shell><section className="dev-detail-hero"><img src={d.image}/><div className="hero-scrim"/><div><p>{d.place}</p><h1>{d.name}</h1><p>From {euro(d.from)}</p></div></section><nav className="anchor-nav"><a href="#overview">Overview</a><a href="#residences">Residences</a><a href="#amenities">Amenities</a><a href="#contact">Enquire</a></nav><section id="overview" className="section shell dev-intro"><p className="eyebrow">A NEW PERSPECTIVE</p><h2>{d.copy}</h2><p>Every detail has been composed to make daily life feel effortless, from the relationship with the landscape to the materials you touch.</p></section><section id="amenities" className="wide-image"><img src={properties[4].image}/><div><h2>Designed around<br/>the way you live.</h2><div>{d.amenities.map(x=><span key={x}><Check/> {x}</span>)}</div></div></section><section id="residences" className="section shell units"><div className="section-heading"><h2>Available residences</h2><p>{d.available} homes currently available</p></div><div className="unit-table"><div className="unit-row head"><span>Residence</span><span>Type</span><span>Interior</span><span>Price</span><span>Status</span></div>{d.units.map(u=><button key={u.unit} disabled={u.status==='Sold'} onClick={()=>setSelected(u.unit)} className={`unit-row ${selected===u.unit?'selected':''}`}><span>{u.unit}</span><span>{u.type}</span><span>{u.size} m²</span><span>{euro(u.price)}</span><span className={`status ${u.status.toLowerCase()}`}>{u.status}</span></button>)}</div></section><section id="contact" className="dev-contact"><div><p className="eyebrow light">PRIVATE APPOINTMENTS</p><h2>Request the full<br/>development details.</h2><p>Receive availability, floor plans and the latest brochure.</p></div><EnquiryForm subject={`${d.name}${selected?`, unit ${selected}`:''}`}/></section></Shell>}
function Agencies(){return <Shell><section className="inner-hero shell"><p className="eyebrow">LOCAL EXPERTISE</p><h1>Meet Portugal’s<br/>property professionals.</h1><p>Explore trusted agencies with deep knowledge of the places they represent.</p></section><section className="section shell agency-grid">{agencies.map(a=><Link to={`/agencies/${a.slug}`} className="agency-card" key={a.slug}><span className="agency-mark">{a.mark}</span><div><p>{a.location}</p><h2>{a.name}</h2><p>{a.about}</p><b>{a.count} active listings <ArrowRight/></b></div></Link>)}</section></Shell>}
function AgencyProfile({slug}){const a=agencies.find(x=>x.slug===slug)||agencies[0];const[favs,setFavs]=useFavs();const ps=properties.filter(p=>p.agency.slug===a.slug);return <Shell><section className="agency-profile shell"><span className="agency-mark large">{a.mark}</span><div><p>{a.location}, Portugal</p><h1>{a.name}</h1><p>{a.about}</p><div className="agency-contacts"><span><Phone/> {a.phone}</span><span><EnvelopeSimple/> {a.email}</span></div></div><button className="button dark">Contact agency</button></section><section className="section shell"><div className="section-heading"><h2>Properties from {a.name}</h2><p>{ps.length} in the prototype collection</p></div><div className="property-grid">{ps.map(p=><PropertyCard key={p.id} p={p} favs={favs} setFavs={setFavs}/>)}</div></section></Shell>}
const agencyNav=[['Overview','/agency'],['Properties','/agency/properties'],['Enquiries','/agency/enquiries'],['Analytics','/agency/analytics'],['Feed Integration','/agency/feed'],['Agency Profile','/agency/profile'],['Settings','/agency/settings']];
function AgencyLayout({children,active}){return <div className="portal"><aside><Logo light/><div className="demo-label">DEMO WORKSPACE</div><nav>{agencyNav.map(([n,p])=><Link className={active===n?'active':''} to={p} key={n}>{n}</Link>)}</nav><Link to="/" className="back">← Public website</Link></aside><main><div className="portal-top"><div><small>Casa Nova Portugal</small><b>MF</b></div></div>{children}</main></div>}
function Sparkline(){return <div className="chart"><div className="chart-bars">{[38,45,42,58,51,67,61,73,68,82,77,91,86,100].map((n,i)=><motion.i key={i} initial={{height:0}} animate={{height:`${n}%`}} transition={{delay:i*.03}}/>)}</div><div className="chart-meta"><span>12 Aug</span><span>Today</span></div></div>}
function Dashboard(){return <AgencyLayout active="Overview"><div className="portal-page"><p className="eyebrow">THURSDAY, 10 SEPTEMBER</p><h1>Good morning, Maria.</h1><p>Here is how your portfolio is performing.</p><div className="metrics">{[['Active listings','84','+3 this month'],['New enquiries','18','6 need a reply'],['Property views','4,821','+12.4%'],['Saved properties','312','+8.1%']].map(([l,n,s])=><div key={l}><span>{l}</span><b>{n}</b><small>{s}</small></div>)}</div><div className="dashboard-grid"><section><div className="panel-title"><div><h2>Listing views</h2><p>Last 30 days · seeded demo analytics</p></div><b>4,821 <small>views</small></b></div><Sparkline/></section><section className="tasks"><h2>Needs your attention</h2><Link to="/agency/enquiries"><span><EnvelopeSimple/></span><div><b>6 new enquiries</b><small>Oldest received 3 hours ago</small></div><ArrowRight/></Link><Link to="/agency/feed"><span><Check/></span><div><b>Feed healthy</b><small>84 listings synced today</small></div><ArrowRight/></Link></section></div></div></AgencyLayout>}
function AgencyProperties(){const[items,setItems]=useState(properties.filter(p=>p.agency.slug==='casa-nova').map(p=>({...p})));return <AgencyLayout active="Properties"><div className="portal-page"><div className="portal-heading"><div><p className="eyebrow">PORTFOLIO</p><h1>Properties</h1><p>{items.length} listings in this demo workspace</p></div><button className="button dark"><Plus/> Add property</button></div><div className="data-list"><div className="data-row head"><span>Property</span><span>Price</span><span>Views</span><span>Status</span><span></span></div>{items.map(p=><div className="data-row" key={p.id}><span className="data-property"><img src={p.image}/><span><b>{p.title}</b><small>{p.city} · {p.ref}</small></span></span><span>{short(p.price)}</span><span>{390+p.id*61}</span><select value={p.status} onChange={e=>setItems(items.map(x=>x.id===p.id?{...x,status:e.target.value}:x))}><option>Live</option><option>Under Offer</option><option>Sold</option><option>Draft</option></select><button onClick={()=>go(`/agency/properties/${p.id}`)}>Edit</button></div>)}</div></div></AgencyLayout>}
function EditProperty({id}){const base=properties.find(p=>p.id===+id)||properties[0];const[p,setP]=useState({...base});const[saved,setSaved]=useState(false);return <AgencyLayout active="Properties"><div className="portal-page"><div className="portal-heading"><div><Link to="/agency/properties" className="muted">← Properties</Link><h1>Edit listing</h1><p>{p.ref}</p></div><button className="button dark" onClick={()=>{localStorage.setItem(`pip-property-${p.id}`,JSON.stringify(p));setSaved(true);setTimeout(()=>setSaved(false),2500)}}>{saved?<><Check/> Saved</>:<>Save changes</>}</button></div><div className="edit-grid"><section><h2>Property details</h2><label>Listing title<input value={p.title} onChange={e=>setP({...p,title:e.target.value})}/></label><div className="field-row"><label>Price<input type="number" value={p.price} onChange={e=>setP({...p,price:+e.target.value})}/></label><label>Status<select value={p.status} onChange={e=>setP({...p,status:e.target.value})}><option>Live</option><option>Under Offer</option><option>Sold</option><option>Draft</option></select></label></div><label>Description<textarea rows="8" value={p.description} onChange={e=>setP({...p,description:e.target.value})}/></label></section><aside><img src={p.image}/><h3>Listing quality</h3><div className="quality"><i/><span>Strong</span></div><p>Your listing has a complete description, location, features and professional imagery.</p></aside></div></div></AgencyLayout>}
function AgencyEnquiries(){const[items,setItems]=useState(seedEnquiries);return <AgencyLayout active="Enquiries"><div className="portal-page"><p className="eyebrow">LEAD INBOX</p><h1>Enquiries</h1><p>Buyer requests across your property portfolio.</p><div className="enquiry-list">{items.map(e=><article key={e.id}><div className="enquiry-person"><span>{e.name.split(' ').map(x=>x[0]).join('')}</span><div><h3>{e.name}</h3><p>{e.country} · {e.time}</p></div></div><div><b>{e.property}</b><p>“{e.message}”</p></div><select value={e.status} onChange={x=>setItems(items.map(q=>q.id===e.id?{...q,status:x.target.value}:q))}><option>New</option><option>Contacted</option><option>Viewing Arranged</option><option>Closed</option></select></article>)}</div></div></AgencyLayout>}
function Feed(){const[syncing,setSyncing]=useState(false);const[last,setLast]=useState('Today, 09:42');const sync=()=>{setSyncing(true);setTimeout(()=>{setSyncing(false);setLast('Just now')},1300)};return <AgencyLayout active="Feed Integration"><div className="portal-page"><p className="eyebrow">DISTRIBUTION</p><h1>Listing feed</h1><p>Keep your portfolio automatically up to date.</p><div className="feed-card"><div className="feed-status"><span><Check/></span><div><small>CONNECTION STATUS</small><h2>Connected</h2><p>XML feed · Casa Nova CRM</p></div><button className="button dark" onClick={syncing?null:sync}>{syncing?'Synchronising…':'Sync now'}</button></div><div className="feed-facts"><div><span>Listings discovered</span><b>84</b></div><div><span>Last synchronisation</span><b>{last}</b></div><div><span>Next synchronisation</span><b>In 52 minutes</b></div></div><div className="sync-summary"><h3>Latest import activity</h3>{[['Added','3'],['Updated','11'],['Removed','2'],['Needs review','1']].map(x=><div key={x[0]}><span>{x[0]}</span><b>{x[1]}</b></div>)}</div><p className="demo-note">Prototype demonstration. Production feed capabilities and validation rules may vary.</p></div></div></AgencyLayout>}
function Analytics(){return <AgencyLayout active="Analytics"><div className="portal-page"><p className="eyebrow">PERFORMANCE</p><h1>Analytics</h1><p>Seeded demo data for presentation purposes.</p><div className="metrics">{[['Search appearances','18,420','+14.2%'],['Listing views','4,821','+12.4%'],['Enquiries','74','+9.1%'],['Conversion rate','1.53%','+0.2%']].map(([l,n,s])=><div key={l}><span>{l}</span><b>{n}</b><small>{s}</small></div>)}</div><div className="dashboard-grid"><section><div className="panel-title"><div><h2>Engagement trend</h2><p>Views across the last 30 days</p></div></div><Sparkline/></section><section><h2>Top listings</h2>{properties.slice(0,4).map((p,i)=><div className="rank" key={p.id}><b>0{i+1}</b><img src={p.image}/><span>{p.title}<small>{821-i*127} views</small></span></div>)}</section></div></div></AgencyLayout>}
function PlaceholderPortal({active}){return <AgencyLayout active={active}><div className="portal-page"><p className="eyebrow">WORKSPACE</p><h1>{active}</h1><div className="empty-panel"><Check/><h2>This workspace is presentation ready.</h2><p>Core prototype controls are represented here without production account infrastructure.</p></div></div></AgencyLayout>}
function Mortgage(){const[price,setPrice]=useState(450000),[deposit,setDeposit]=useState(20),[years,setYears]=useState(30),[rate,setRate]=useState(3.3);const loan=price*(1-deposit/100),m=rate/1200,pay=m?loan*m*(1+m)**(years*12)/((1+m)**(years*12)-1):loan/(years*12);return <Shell><section className="tool-page shell"><div><p className="eyebrow">BUYER SERVICES</p><h1>Plan the numbers.<br/>Picture the move.</h1><p>Explore an indicative monthly mortgage payment for a Portuguese property.</p></div><div className="calculator"><label>Property price <b>{euro(price)}</b><input type="range" min="150000" max="2000000" step="25000" value={price} onChange={e=>setPrice(+e.target.value)}/></label><label>Deposit <b>{deposit}%</b><input type="range" min="10" max="60" step="5" value={deposit} onChange={e=>setDeposit(+e.target.value)}/></label><div className="field-row"><label>Term<select value={years} onChange={e=>setYears(+e.target.value)}><option value="20">20 years</option><option value="25">25 years</option><option value="30">30 years</option><option value="35">35 years</option></select></label><label>Interest rate<input type="number" step="0.1" value={rate} onChange={e=>setRate(+e.target.value)}/></label></div><div className="payment"><span>Estimated payment</span><b>{euro(Math.round(pay))}<small> / month</small></b><p>Loan amount {euro(loan)} · Indicative only</p></div><button className="button dark">Get mortgage assistance <ArrowRight/></button></div></section></Shell>}
function Legal(){const[stage,setStage]=useState('');return <Shell><section className="inner-hero shell"><p className="eyebrow">BUYING IN PORTUGAL</p><h1>A clear path<br/>from search to keys.</h1><p>Understand the legal journey and find professional support at the right moment.</p></section><section className="section shell journey">{['Property selected','Legal due diligence','Promissory contract','Financing','Notary and deed','Property registration'].map((x,i)=><div key={x}><b>{String(i+1).padStart(2,'0')}</b><span>{x}</span></div>)}</section><section className="support-form"><div><h2>What stage are you at?</h2><p>Choose the option that best describes your move.</p><div className="stage-options">{['I have not found a property','I have made an offer','I need a lawyer','I need a notary','I am buying from overseas'].map(x=><button className={stage===x?'active':''} onClick={()=>setStage(x)} key={x}>{x}<Check/></button>)}</div></div><EnquiryForm subject={stage||'legal support in Portugal'}/></section></Shell>}
function Favourites(){const[favs,setFavs]=useFavs();const ps=properties.filter(p=>favs.includes(p.id));return <Shell><section className="inner-hero shell"><p className="eyebrow">YOUR SHORTLIST</p><h1>Saved properties.</h1><p>Keep the homes that feel worth another look.</p></section><section className="section shell">{ps.length?<div className="property-grid">{ps.map(p=><PropertyCard key={p.id} p={p} favs={favs} setFavs={setFavs}/>)}</div>:<div className="empty"><Heart/><h2>Your shortlist is empty.</h2><p>Save properties as you explore to keep them here.</p><Link className="button dark" to="/properties">Explore properties</Link></div>}</section></Shell>}
function Router(){const[path,setPath]=useState(location.pathname+location.search);React.useEffect(()=>{const fn=()=>setPath(location.pathname+location.search);addEventListener('popstate',fn);return()=>removeEventListener('popstate',fn)},[]);const clean=path.split('?')[0],parts=clean.split('/').filter(Boolean);if(clean==='/')return <Home/>;if(clean==='/properties')return <Properties/>;if(parts[0]==='properties'&&parts[1])return <PropertyDetail slug={parts[1]}/>;if(clean==='/developments')return <Developments/>;if(parts[0]==='developments'&&parts[1])return <DevelopmentDetail slug={parts[1]}/>;if(clean==='/agencies')return <Agencies/>;if(parts[0]==='agencies'&&parts[1])return <AgencyProfile slug={parts[1]}/>;if(clean==='/agency')return <Dashboard/>;if(clean==='/agency/properties')return <AgencyProperties/>;if(parts[0]==='agency'&&parts[1]==='properties'&&parts[2])return <EditProperty id={parts[2]}/>;if(clean==='/agency/enquiries')return <AgencyEnquiries/>;if(clean==='/agency/analytics')return <Analytics/>;if(clean==='/agency/feed')return <Feed/>;if(clean==='/agency/profile')return <PlaceholderPortal active="Agency Profile"/>;if(clean==='/agency/settings')return <PlaceholderPortal active="Settings"/>;if(clean==='/mortgage')return <Mortgage/>;if(clean==='/legal-support')return <Legal/>;if(clean==='/account/favourites')return <Favourites/>;return <Home/>}
createRoot(document.getElementById('root')).render(<Router/>);
